# Cambium

Cambium is "Rails for generation engineering": readable Ruby DSL (`*.cmb.rb`) that compiles to an auditable IR, executed by a runtime with typed contracts, tools, tracing, and repair loops.

This repo currently contains a v0.1 scaffold + genesis package.
